namespace powerbi.extensibility.visual {

    export enum TextFunctionType {
        none = <any>"none",
        absolute = <any>"absolute",
        truncate = <any>"truncate",
        round = <any>"round",
        ceiling = <any>"ceiling",
        floor = <any>"floor"
    }
}